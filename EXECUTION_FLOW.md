---
name: design-review-execution-flow
description: 设计检视执行流程单（Execution Runbook）—— SKILL.md 的执行侧伴侣文档。AI 在每次检视任务中必须以本文件为行动主线，以 SKILL.md 为规则参考，二者缺一不可。
---

# 设计检视执行流程单 (Execution Runbook)

> [!IMPORTANT]
> ## 文档定位与分工
>
> | 文档 | 层级 | 用途 |
> |---|---|---|
> | **`SKILL.md`** | 规范层 | 定义为什么、边界约束、评分规则、禁止行为——理解层面的权威来源 |
> | **本文件（`EXECUTION_FLOW.md`）** | 执行层 | 定义每步做什么、输出什么、按什么顺序——行动层面的唯一执行基准 |
>
> **AI 必须在每次检视任务开始时同时加载两份文档**，以本文件为行动主线逐步推进；遇到规则细节、定级标准、枚举白名单时，查阅 SKILL.md 对应章节。

---

## ▶ 阶段 0：启动判断（任务接收后立即执行，不得跳过）

### □ Step 0.1 — 检视对象类型判别

| 类型 | 特征 | 置信度基准 |
|---|---|---|
| **可交互前端 Demo** | 前端完整可交互，数据为 mock，无真实后端 | 按「完整状态」严格检视，所有前端状态均高置信准入 |
| **可交互真实系统** | 有真实后端与真实数据 | 所有观察高置信准入 |
| **纯静态视觉稿 / 截图** | 仅有静态图，无交互实现 | 交互类观察降级为「待验证假设 / 中置信」 |

📌 详细规则：`SKILL.md §0.0 检视对象类型判别`

---

### □ Step 0.2 — 执行路径路由（Tier 路由）

- **→ Tier 1（主模式 Path 1）**：Context 中存在以下任意一种工具：`browser_subagent` / `chrome_devtools` / `playwright` / `puppeteer` / 支持 navigate + screenshot + click 的工具组合 → **走浏览器走查模式**
- **→ Tier 2（降级模式 Path 2）**：无任何浏览器工具 → **走静态代码分析模式**，报告头部**显式声明降级原因**

📌 详细规则：`SKILL.md §0 执行路径选择表格`

---

### □ Step 0.3 — 输出 4 维度确认告知卡片（**必须第一时间输出**）

```
📋 本次设计检视默认开启以下 4 大维度全量审查：
1. 【维度 1：体验目标达成检视】
2. 【维度 2：产品理念契合度检视】
3. 【维度 3：常规交互 Checklist 检视】
4. 【维度 4：方案完备性检视】

💬 前置信息补充建议（可选）：
- 补充【核心体验目标 + 关键任务剧本】→ 提升维度 1 精度
- 补充【需求背景与期望效果】→ 提升维度 2 精度

⚙️ 如无调整，将直接开展全维度走查。
```

📌 详细规则：`SKILL.md §1.1`

---

## ▶ 阶段 1：前置准备（走查开始前全部完成，不得边准备边走查）

### □ Step 1.1 — 确定维度 1 & 3 自适应检视角色

- 根据被检产品的目标用户画像，选择终端用户角色（如：网络安全运营工程师 / 安全管理员 / 普通管理员）
- **在对话中明确说明角色选择原因**

📌 详细规则：`SKILL.md §1.2 检视角色定位表格`

---

### □ Step 1.2 — 加载维度 3 检视基准（**完成后方可开始走查**）

**步骤 A（读取 + 自动匹配）**
- 读取并加载 `references/02_设计检视Checklist.md`
- 基于 §0「快速匹配指南与索引表」，对照被检界面功能特征，识别并激活适用特定子维度（子维度 5-26）

**步骤 B（显式输出激活清单）**
```
【维度 3 激活子维度清单】
- 公共子维度 1：视觉与规范一致性（必检）
- 公共子维度 2：交互效率与简单易用（必检）
- 公共子维度 3：认知、反馈与防错机制（必检）
- 公共子维度 4：文案与专业表述审查（必检）
- 特定子维度 [编号]：[名称]（条件匹配）
```

📌 详细规则：`SKILL.md §1.1 维度 3 强制前置动作`

---

### □ Step 1.3 — 加载体验目标与愿景基准

- 读取 `references/01_体验目标制定.md`（用户未提供体验目标时，据此自动推导）
- 读取 `references/04_SASE体验愿景.md`（维度 2 方案级审视的对照基准）

---

### □ Step 1.4 — 建立任务链路与验证计划

输出：
1. **主任务链路**：目标用户入口 → 核心完成的 1-3 个 Job → 上下游闭环节点
2. **边缘 & 异常场景走查路线**：空白零数据 / 极长文本溢出 / 海量数据 / 网络异常 / 50x 错误 / 无权限页面

📌 详细规则：`SKILL.md §1.3`

---

## ▶ 阶段 2：第一阶段走查（维度 1 & 3 合并，浏览器动态走查）

> 🔵 **角色**：Step 1.1 确定的自适应终端用户视角
> ⚠️ 「合并走查」= 共用浏览器会话，**不等于**同时分裂注意力

### □ Step 2.x — 对每个界面 / 表单 / 抽屉执行「焦点切换六步法」

**↓ 步骤 A：打开界面**
通过浏览器工具导航至目标页面 / 点击触发抽屉 / 弹窗

---

**↓ 步骤 B：焦点① 维度 1 任务尝试（优先执行，未完成前禁止切入 Checklist）**
- [ ] 以测试角色完整尝试当前界面的操作任务
- [ ] 记录：任务能否完成 / 哪步受阻 / 失败点描述
- [ ] 替代路径检查：是否有隐藏入口 / 说明面板 / 相邻页面闭环 / 1-2 步可达的替代方案
- [ ] 候选问题标记 `[维度1]` 暂存

---

**↓ 步骤 C：焦点② 维度 3 Checklist 扫描（保持界面不关闭，切换焦点）**
- [ ] 逐条对照 Step 1.2 的**激活子维度清单**执行核查（严禁以泛化记忆代替逐条核查）
- [ ] 候选问题标记 `[维度3]` 暂存，**严禁与维度 1 问题混排**

---

**↓ 步骤 D：正式问题准入过滤（每个候选问题必须通过全部 5 项）**

| # | 准入项 | 核查要点 |
|---|---|---|
| 1 | 任务相关 | 能否对应用户真实任务或关键链路？ |
| 2 | 已尝试 | AI 是否实际操作过并触发了该问题？ |
| 3 | 已排除替代路径 | 不存在 1-2 步内可发现的清晰替代方案 |
| 4 | 业务影响明确 | 能说清对效率 / 准确性 / 风险控制的具体影响 |
| 5 | 建议可落地 | 改进方向具体可执行 |

> **P1/P2 额外必须过「四问反向辩驳」**（阻塞举证 → 损耗举证 → 降级测试 → 反证测试）
> 未过准入门槛 → 移入「待验证假设」区，不参与评分扣分
> 
> 🔒 **【纯追加制 Append-Only & 严禁槽位替换】**：问题数量不设任何上限，通过准入门槛的问题必须**全部无条件并列追加**（8个、12个、20个均可）。**严禁**设定固定槽位（如固定 6 个），**严禁**在多轮对话或问题补充时把已有问题删掉/替换掉来维持固定数量！

📌 详细规则：`SKILL.md §3 正式问题准入门槛 & Append-Only 纯追加原则`

---

**↓ 步骤 E：同步批注截图（在当前界面关闭前完成，不得事后补做）**
- [ ] 记录视口尺寸与 `devicePixelRatio`
- [ ] 用 `getBoundingClientRect` 获取问题区域 CSS 坐标
- [ ] 坐标 × dpr → 调用 `scripts/auto_annotate.py <截图> '<json批注>'` → 落图至 `Reports/assets`

> ⛔ **重复操作熔断（3 次上限）**：任意操作失败最多重试 3 次：
> - 截图 / 批注失败 → 标准占位图，附注「截图获取失败·已熔断（3次）」
> - 页面导航失败 → 降级为该页面静态代码分析，报告头部声明
> - 工具调用异常 → 跳过当前节点，在过程备忘中标记「未覆盖 / 工具熔断」

---

**↓ 步骤 F：输出过程备忘快照（上下文使用量超 70% 时强制输出）**
```
【过程备忘快照】
已完成节点：[节点名] ✅
已发现问题：[维度X] [标题] [P级] [截图路径]
下一步：[待执行节点]
```

📌 详细规则：`SKILL.md §1.4 过程备忘快照机制`

---

### □ Step 2.last — 任务链路打勾收尾

- [ ] 对 Step 1.4 的每条链路节点标记「已走查 / 未覆盖及原因」
- [ ] 未覆盖节点在报告「观察备注」中显式声明，不得静默跳过

---

## ▶ 阶段 3：第二阶段审视（维度 2，独立执行）

> 🔴 **🔒 固定角色：精通安全行业（SASE / SOC / DEM 等）的产品专家与设计专家**
> 本阶段不得退化为普通用户视角或泛化常识判断

### □ Step 3.1 — 确认已加载维度 2 基准

- [ ] 确认 `references/04_SASE体验愿景.md` 已在 Step 1.3 加载

### □ Step 3.2 — 方案级高维审视（🔒 场景自适应理念锚定与高维拷问 SOP）

必须先识别需求类型，再动态映射核心愿景并执行场景化高维负向拷问：
- [ ] **步骤 A（类型判别）**：明确需求属于【运维排障类】/【策略配置类】/【运营分析类】/【办公终端类】中的哪一种（或多组合）；
- [ ] **步骤 B（理念映射与高维负向拷问）**：
  - 🛠️ **运维排障类**（锚定高效运维/自愈/掌控）：拷问 AI Copilot 替代、主动自愈引导、Actionable 一键闭环处置、物理空间/AP 聚合看板；
  - ⚙️ **策略配置类**（锚定意图驱动/极简配置/去技术化）：拷问业务意图声明式配置、参数智能推荐、多步表单极简压缩、冲突预检与确定性；
  - 📊 **运营分析类**（锚定全面掌控/态势叙事/预测归因）：拷问大盘叙事化与风险聚类、趋势预判与收敛归因、1步直达处置；
  - 💻 **办公终端类**（锚定自然办公/无感防护/优雅降级）：拷问静默低侵入、报错去技术化、阻断场景一键申诉业务连续性闭环；
- [ ] **步骤 C（方案级解法输出）**：命中核心理念断层必须列为 P1/P2 方案级重构解法；严禁将微观控件/路由参数问题错挂为维度 2。

📌 详细规则：`SKILL.md §1.1 维度 2 场景自适应理念锚定与高维拷问协议`

---

## ▶ 阶段 4：第三阶段扫描（维度 4，静态代码扫描）

> 🔴 **🔒 固定角色：精通安全行业（SASE / SOC / DEM 等）的产品专家与设计专家**

### □ Step 4.1 — 读取完备性 Checklist

- [ ] 读取 `references/06_方案完备性检视Checklist.md`，加载全部 12 类检视项目

### □ Step 4.2 — 静态代码逐项扫描与 12 类全状态矩阵强制输出

**必须输出 12 类项目完整核查打勾矩阵（No Table = No Pass）**，重点扫描系统级完备性硬伤：
1. `Deep Linking 与状态持久化`（刷新/分享 URL 能否还原排障案发现场）
2. `跨角色安全数据脱敏`（核心资产 IP、工号掩码与 RBAC 权限隔离）
3. `加载态骨架屏 (Skeleton)` 与局部 Spin 遮罩
4. `零数据空白态插槽` 与清空重置行动引导
5. `极值边界与超长文本 Tooltip 完整覆盖`

> ⛔ **严禁敷衍凑数**：严禁仅抓一个 Tooltip 截断等 P4 视觉瑕疵来敷衍维度 4；非满分结论必须指出系统级结构性缺陷。
> 
> 📊 **维度 4 批量轻微问题表格化展示规则**：
> - **触发条件（必须同时满足以下 3 项，缺一不可）**：
>   1. 识别问题**仅属于维度 4（方案完备性检视）**单维度；
>   2. 问题级别**均为 P3 / P4 型**（无 P1/P2 严重阻塞或高危操作缺失）；
>   3. 问题数量**大于 3 个**（$\ge 4$ 个）。
> - **展示形式**：以高密度**汇总表格**展示（收拢卡片与占位图），表格字段为：`页面`、`缺失类型`、`缺失描述`。
> 
> ⛔ **时间熔断**：单文件行数 > 5000 行，或累计耗时 > 8 分钟 → 触发熔断，输出定性结论

📌 详细规则：`SKILL.md §2 Path 1 静态逻辑扫描 & 维度 4 耗时自动熔断机制`

---

## ▶ 阶段 5：报告生成

### □ Step 5.1 — 生成结构化报告

- [ ] **Tier 1**：使用 `references/05_HTML检视报告模板.html` 生成 HTML 报告
- [ ] **Tier 2 降级**：生成 Markdown 报告，头部显式声明降级原因
- [ ] 每个正式问题卡片包含全部 10 项字段（*注：若命中维度 4 批量 P3/P4 规则，以表格汇总呈现*）

📌 详细规则：`SKILL.md §4 正式问题卡片字段规范`

### □ Step 5.2 — 填写四维定性结论（严格匹配枚举白名单，禁止同义词替换）

| 维度 | 合法枚举值 |
|---|---|
| 维度 1 | `超预期达成` / `达成` / `基础达成` / `未达成` / `未显式走查` |
| 维度 2 | `符合` / `基本符合，有优化空间` / `部分符合` / `不符合` / `未显式走查` |
| 维度 3 | `通过` / `基本通过，有优化空间` / `基础达成` / `未达成` / `未显式走查` |
| 维度 4 | `完备` / `基本完备，存在边缘缺失` / `完备性不足` / `严重缺失` / `未显式走查` |

> **硬约束**：非满分结论对应维度问题数必须 ≥ 1

📌 详细规则：`SKILL.md §4 报告结论写作规范`

### □ Step 5.3 — 附录评分说明表

- [ ] 在报告最下方附带【体验评分与问题等级说明表】

---

## ▶ 阶段 6：报告准出自检（全部通过方可交付）

| # | 自检项 | 通过标准 |
|---|---|---|
| ① | 截图引用 | 每个问题卡片为真实截图，无占位图残留 |
| ② | 枚举白名单 | 四大维度结论 100% 等于白名单字符串 |
| ③ | 结论-问题数一致 | 非满分结论对应维度问题数 ≥ 1 |
| ④ | 维度 2 场景化高维审视 | 已完成需求场景判别（排障/配置/运营/终端），并提出具体的方案级重构解法，无微观交互错挂 |
| ⑤ | 维度 4 矩阵闭环 | 已输出 12 类完备性矩阵打勾表，非满分结论已包含系统级完备性硬伤（如 Deep Linking/脱敏/骨架屏） |
| ⑥ | 底部评分表 | 报告最下方已附带说明表 |
| ⑦ | 批注落地 | 维度 1~3 正式问题均有批注截图；无批注能力时已显式声明 |
| ⑧ | 问题纯追加 Append-Only | 问题清单无固定数量槽位（绝不在 6 个卡片中替换淘汰），所有通过准入的问题已全量并列入清单并客观算分 |

✅ 全部通过 → 交付报告

📌 详细规则：`SKILL.md §4 报告准出自检 Checklist`

---

*本文件与 `SKILL.md` 共同构成设计检视技能的完整执行体系。规则更新时两份文档须同步维护。*
